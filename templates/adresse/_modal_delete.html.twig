<!-- Modal -->
<div class="modal fade" id="adresseModal" tabindex="-1" role="dialog"
     aria-labelledby="adresseModal" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-body">
                Supprimer <strong><span id="modal-body-nom"></span></strong> ?
            </div>
            <div class="modal-footer">
                {{ form_start(form_delete) }}
                <input type="hidden" id="modal-body-input" name="idadresse"/>
                <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                <button type="submit" class="btn btn-danger">Supprimer</button>
                {{ form_end(form_delete) }}
            </div>
        </div>
    </div>
</div>
<!-- end Modal -->