{% extends 'admin/layout.html.twig' %}

{% block body %}

    <div class="page-header">
        <h3>Commande {{ commande }}</h3>
    </div>

    <div class="alert alert-info">
        Le commerçant doit valider la commande avant que le logisticien ne prenne la main
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Valider la commande" class="btn btn-default"/>
     {{ form_end(form) }}

{% endblock %}
